const A = ({ children, href, className }) => (
	<a href={href} className={className}>
		{children}
		<style jsx>{`
      a {

      }
      a:hover {

      }
      a.small {

      }
      a.small:hover {

      }
    `}</style>
	</a>
)

export default A
